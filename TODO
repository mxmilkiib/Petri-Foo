TODO list for MOD1 release :
----------------------------
bug : DC is output when envelope of filter returns to 0 (if cutoff=0, attack!=0, decay=0)


ideas for MOD2 :
----------------
multi out
automatic root note
LV2 plugin, managed like LinuxSampler
